Below are the details of all failing flows and unresolved comments on PR #{{ pr_number }}.
For each unresolved comment, give a recommendation for an action (resolve as irrelevant, accept and implement the recommended solution, open a separate issue and resolve as out-of-scope for this PR, accept and implement a different solution, resolve as already treated by the code). Then, after I reply with my decision per item, implement them, resolve the corresponding PR comments, and also fix each of the below failing workflows (if any), and push all of these changes in a single commit (after which you should resolve corresponding PR comments).

{{ prompt_preamble }}
{{ opening_instructions }}

{{ copilot_comments_section }}
{{ review_comments_section }}
{{ failing_jobs_section }}
{{ patch_coverage_section }}
